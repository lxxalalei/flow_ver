# USER.md

- **Timezone:** Asia/Shanghai
- **Preferred language:** 中文
- **Interaction:** 通过自然语言提出学习资源需求、比较候选、选择或取消获取、查询任务和资料库。

## 体验要求

- 系统分别理解当前对话者和资源服务对象；两者都可以暂时未知，不能根据其中一项自动推断另一项。
- 搜索由资源对象、目标和用户明确约束驱动；其余信息只在确实影响搜索方向、内容尺度或筛选标准时追问。
- 年龄、年级、教材版本等不是固定必问项；只有不同答案会显著改变结果时才澄清。
- 用户不需要理解 Agent、Skill、MCP 或业务 ID。
- 先给结论和可操作选项，避免暴露内部实现细节。
- 只有用户已经明确选中资源并表达“下载 / 保存 / 获取下来”等意图时才执行获取；对象和意图都清楚时，不重复要求形式化二次确认。
- 平台是否需要认证以当前具体能力的真实结果为准；不要因为平台支持登录就把登录当成搜索/下载前置步骤。
- 遇到未接入、真实认证要求或当前不可获取的平台时说明实际能力限制，并在用户目标需要时提供替代路线。

## 设计授权与长期偏好

- 以最终教育/学习资源体验和业务闭环为目标，不要求兼容旧七个 Skill、六阶段目录或 Stage JSON。
- 允许对 active Skill 和 MCP 进行重构、替换；旧实现只作为领域知识、案例和回归证据来源，不在旧版本上机械打补丁。
- 复杂任务可以使用轻量计划和边界清晰的临时并行 Agent；主 Agent 必须重新汇总判断，不把临时搜索计划固化成持久业务状态。
