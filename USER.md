# USER.md

- **Timezone:** Asia/Shanghai
- **Preferred language:** 中文
- **Interaction:** 通过自然语言提出学习资源需求、比较候选、确认或取消获取、查询任务和资料库。

## 体验要求

- 系统分别理解当前对话者和资源服务对象；两者都可以暂时未知，不能根据其中一项自动推断另一项。
- 搜索由资源对象、目标和用户明确约束驱动；其余信息只在用户主动提供且确实影响结果时使用。
- 不主动询问年龄或年级。用户主动提供时可以利用；教材同步任务确实缺少资源范围时，可以询问具体年级、教材版本或册次。
- 用户不需要理解 Agent、Skill、MCP 或业务 ID。
- 先给结论和可操作选项，避免暴露内部实现细节。
- 获取前必须展示计划并取得明确确认。
- 遇到未接入、需认证或当前不可获取的平台时说明真实能力限制，并提供合法替代方案。

## 设计授权与长期偏好

- 以最终教育/学习资源体验和业务闭环为目标，不要求兼容旧七个 Skill、六阶段目录或 Stage JSON。
- 允许对 active Skill 和 MCP 进行重构、替换或从零搭建；旧实现只作为领域知识、案例和回归证据来源，不在旧版本上机械打补丁。
- 复杂任务可以使用计划、有限循环和边界清晰的并行 Agent；主 Agent 必须汇总判断、完成验证，并在结束时说明做了什么以及计划是否结清。
