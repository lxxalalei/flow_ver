name: 0037 part2 compatibility cutover

on:
  push:
    branches:
      - codex/growth-resource-taxonomy-rework

permissions:
  contents: write

jobs:
  cutover:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply second-stage cleanup
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          root = Path('.')

          # 1) AcquisitionRequest becomes a real small execution DTO rather than
          #    a subclass with empty authority slots.
          simple_path = root / 'mcp/education-resources/src/education_resource_mcp/acquisition/simple.py'
          simple_path.write_text(r'''"""Simplified exact-provider acquisition seam.

A Provider receives only the execution facts needed to perform the operation.
Descriptor, readiness, eligibility and digest credentials are not part of this
boundary.
"""

from __future__ import annotations

from collections.abc import Mapping
from pathlib import Path
import copy
import threading
from typing import Any

from .models import AcquisitionResult, AcquisitionStrategy, CAPABILITY_SCOPES
from .router import AcquisitionRouter as _LegacyAcquisitionRouter
from .router import ProviderRegistration


class AcquisitionRequest:
    """Server-authored execution request containing only business facts."""

    __slots__ = (
        "job_id",
        "resource",
        "strategy",
        "provider_id",
        "provider_version",
        "planned_scope",
        "representation_id",
        "preferred_container",
        "cancel_event",
        "jobs_root",
    )

    def __init__(
        self,
        *,
        job_id: str,
        resource: Mapping[str, Any],
        strategy: AcquisitionStrategy | str,
        provider_id: str,
        provider_version: str,
        planned_scope: str,
        representation_id: str,
        preferred_container: str = "original",
        cancel_event: threading.Event | None = None,
        jobs_root: Path | None = None,
    ) -> None:
        if not isinstance(job_id, str) or not job_id:
            raise ValueError("job_id must be a non-empty server identifier")
        if not isinstance(resource, Mapping):
            raise TypeError("resource must be a mapping")
        resource_id = resource.get("resource_id")
        if not isinstance(resource_id, str) or not resource_id:
            raise ValueError("resource must contain resource_id")
        selected_strategy = AcquisitionStrategy.from_value(strategy)
        if not isinstance(provider_id, str) or not provider_id:
            raise ValueError("provider_id must be non-empty")
        if not isinstance(provider_version, str) or not provider_version:
            raise ValueError("provider_version must be non-empty")
        if planned_scope not in CAPABILITY_SCOPES:
            raise ValueError("planned_scope must be a declared acquisition scope")
        if not isinstance(representation_id, str) or not representation_id:
            raise ValueError("representation_id must be non-empty")
        if preferred_container not in {
            "original", "pdf", "epub", "mp4", "mp3", "html", "text"
        }:
            raise ValueError("unsupported preferred container")
        event = cancel_event or threading.Event()
        if not isinstance(event, threading.Event):
            raise TypeError("cancel_event must be threading.Event")
        if not isinstance(jobs_root, Path):
            raise TypeError("jobs_root must be a server-provided pathlib.Path")
        resolved_root = jobs_root.resolve(strict=False)
        if not resolved_root.is_absolute() or ".." in resolved_root.parts:
            raise ValueError("jobs_root must be an absolute server-controlled root")

        self.job_id = job_id
        self.resource = copy.deepcopy(dict(resource))
        self.strategy = selected_strategy
        self.provider_id = provider_id
        self.provider_version = provider_version
        self.planned_scope = planned_scope
        self.representation_id = representation_id
        self.preferred_container = preferred_container
        self.cancel_event = event
        self.jobs_root = resolved_root

    @property
    def resource_id(self) -> str:
        return str(self.resource["resource_id"])

    def mutable_resource(self) -> dict[str, Any]:
        return copy.deepcopy(dict(self.resource))

    def to_dict(self) -> dict[str, Any]:
        return {
            "job_id": self.job_id,
            "resource_id": self.resource_id,
            "strategy": self.strategy.kind,
            "provider_id": self.provider_id,
            "provider_version": self.provider_version,
            "planned_scope": self.planned_scope,
            "representation_id": self.representation_id,
            "preferred_container": self.preferred_container,
        }


class AcquisitionRouter(_LegacyAcquisitionRouter):
    """Exact routing without capability-authority credentials."""

    def acquire(self, request: AcquisitionRequest) -> AcquisitionResult:
        if not isinstance(request, AcquisitionRequest):
            raise TypeError("router.acquire expects AcquisitionRequest")
        if request.cancel_event.is_set():
            return self._planned_failure(request, "JOB_CANCELLED", "获取任务已取消")

        registration, failure = self._resolve_registration(request)
        if failure is not None:
            return failure
        assert registration is not None
        provider = registration.provider
        if request.strategy is AcquisitionStrategy.DIRECT_FILE:
            result = self._call_download_provider(
                request,
                provider,  # type: ignore[arg-type]
                provider_strategy="direct",
                result_strategy=AcquisitionStrategy.DIRECT_FILE,
                provider_id=registration.provider_id,
            )
        elif request.strategy is AcquisitionStrategy.WEB_MATERIALIZE:
            result = self._call_result_provider(
                request,
                provider,  # type: ignore[arg-type]
                strategy=AcquisitionStrategy.WEB_MATERIALIZE,
                method_name="materialize",
                provider_id=registration.provider_id,
            )
        elif request.strategy is AcquisitionStrategy.WEB_CAPTURE:
            if callable(getattr(provider, "capture", None)):
                result = self._call_result_provider(
                    request,
                    provider,  # type: ignore[arg-type]
                    strategy=AcquisitionStrategy.WEB_CAPTURE,
                    method_name="capture",
                    provider_id=registration.provider_id,
                )
            elif callable(getattr(provider, "download", None)):
                result = self._call_download_provider(
                    request,
                    provider,  # type: ignore[arg-type]
                    provider_strategy="webpage",
                    result_strategy=AcquisitionStrategy.WEB_CAPTURE,
                    provider_id=registration.provider_id,
                )
            else:
                result = AcquisitionResult.failed(
                    request.strategy,
                    "PROVIDER_UNAVAILABLE",
                    "已绑定的网页捕获器不可用",
                )
        else:  # pragma: no cover
            result = AcquisitionResult.failed(
                request.strategy,
                "UNSUPPORTED_ACQUISITION_STRATEGY",
                "不支持的获取策略",
            )
        return self._bind_result(request, registration, result)

    @staticmethod
    def _authority_kwargs(
        request: AcquisitionRequest,
        registration: ProviderRegistration | None = None,
    ) -> dict[str, Any]:
        values: dict[str, Any] = {
            "planned_provider_id": request.provider_id,
            "planned_provider_version": request.provider_version,
            "planned_scope": request.planned_scope,
            "representation_id": request.representation_id,
        }
        if registration is not None:
            values.update(
                {
                    "provider_id": registration.provider_id,
                    "provider_version": registration.provider_version,
                    "actual_scope": request.planned_scope,
                }
            )
        return values


__all__ = ["AcquisitionRequest", "AcquisitionRouter"]
''', encoding='utf-8')

          # 2) Store migration 9 physically removes the superseded authority tables.
          storage_path = root / 'mcp/education-resources/src/education_resource_mcp/simple_storage.py'
          text = storage_path.read_text(encoding='utf-8')
          text = text.replace('LATEST_SCHEMA_VERSION = 8', 'LATEST_SCHEMA_VERSION = 9', 1)
          old_apply = '''    def _apply_migrations(self) -> None:\n        super()._apply_migrations()\n        with self.transaction(immediate=True) as connection:\n            applied = connection.execute(\n                "SELECT 1 FROM schema_migrations WHERE version = 8"\n            ).fetchone()\n            if applied is not None:\n                return\n            self._migration_simple_acquisition(connection)\n            connection.execute(\n                "INSERT INTO schema_migrations(version, name, applied_at) VALUES (8, ?, ?)",\n                ("simple_acquisition_state", utc_now()),\n            )\n            connection.execute("PRAGMA user_version = 8")\n'''
          new_apply = '''    def _apply_migrations(self) -> None:\n        super()._apply_migrations()\n        with self.transaction(immediate=True) as connection:\n            applied = connection.execute(\n                "SELECT 1 FROM schema_migrations WHERE version = 8"\n            ).fetchone()\n            if applied is None:\n                self._migration_simple_acquisition(connection)\n                connection.execute(\n                    "INSERT INTO schema_migrations(version, name, applied_at) VALUES (8, ?, ?)",\n                    ("simple_acquisition_state", utc_now()),\n                )\n                connection.execute("PRAGMA user_version = 8")\n        with self.transaction(immediate=True) as connection:\n            applied = connection.execute(\n                "SELECT 1 FROM schema_migrations WHERE version = 9"\n            ).fetchone()\n            if applied is None:\n                self._migration_drop_legacy_acquisition_authority(connection)\n                connection.execute(\n                    "INSERT INTO schema_migrations(version, name, applied_at) VALUES (9, ?, ?)",\n                    ("drop_legacy_acquisition_authority", utc_now()),\n                )\n                connection.execute("PRAGMA user_version = 9")\n'''
          if old_apply not in text:
              raise SystemExit('simple_storage _apply_migrations shape changed')
          text = text.replace(old_apply, new_apply, 1)
          marker = '    @staticmethod\n    def _normalize_plan_items('
          cleanup = '''    def _migration_drop_legacy_acquisition_authority(\n        self, connection: sqlite3.Connection\n    ) -> None:\n        """Drop the v6/v7 acquisition proof tables after v8 backfill."""\n\n        self._execute_statements(\n            connection,\n            """\n            DROP TABLE IF EXISTS job_execution_items;\n            DROP TABLE IF EXISTS acquisition_outcomes;\n            DROP TABLE IF EXISTS download_plan_items;\n            DROP TABLE IF EXISTS eligibility_decisions;\n            DROP TABLE IF EXISTS capability_readiness_snapshots;\n            """,\n        )\n\n'''
          if marker not in text:
              raise SystemExit('simple_storage normalize marker missing')
          text = text.replace(marker, cleanup + marker, 1)
          text = text.replace(
              '''        plan_items: list[dict[str, Any]] | None = None,\n        **legacy_kwargs: Any,\n    ) -> dict[str, Any]:\n        # capability_items is intentionally not accepted as authority. It may\n        # arrive only from a stale internal caller during a rolling upgrade.\n        if plan_items is None and isinstance(legacy_kwargs.get("capability_items"), list):\n            raise ValueError("legacy_capability_items_not_supported")\n''',
              '''        plan_items: list[dict[str, Any]] | None = None,\n    ) -> dict[str, Any]:\n''',
              1,
          )
          text = text.replace(
              '''        *,\n        bindings: Mapping[str, Any],\n        **legacy_kwargs: Any,\n    ) -> tuple[dict[str, Any], bool]:\n        del legacy_kwargs\n''',
              '''        *,\n        bindings: Mapping[str, Any],\n    ) -> tuple[dict[str, Any], bool]:\n''',
              1,
          )
          old_decode = '''        item["resource"] = resource\n        item["representation"] = _load(item.pop("representation_json"), {})\n        # Temporary keys let the inherited runner call the simplified\n        # AcquisitionRequest without fabricating persistent authority facts.\n        item["capability_scope"] = item["planned_scope"]\n        for key in (\n            "execution_binding_digest", "capability_id", "descriptor_version",\n            "descriptor_digest", "readiness_snapshot_id", "readiness_digest",\n            "eligibility_id", "eligibility_digest",\n        ):\n            item[key] = None\n        return item\n'''
          new_decode = '''        item["resource"] = resource\n        item["representation"] = _load(item.pop("representation_json"), {})\n        return item\n'''
          if old_decode not in text:
              raise SystemExit('simple_storage job item compatibility shape changed')
          text = text.replace(old_decode, new_decode, 1)
          text = text.replace(
              '''\n    # Compatibility for the existing job runner during the thin-service cutover.\n    get_job_execution_items = get_job_items\n''',
              '',
              1,
          )
          storage_path.write_text(text, encoding='utf-8')

          # 3) Active service owns its Job runner and JobStatus projection directly.
          service_path = root / 'mcp/education-resources/src/education_resource_mcp/simple_service.py'
          text = service_path.read_text(encoding='utf-8')
          text = text.replace(
              'from .acquisition import AcquisitionRouter, AcquisitionStrategy, ProviderRegistration',
              'from .acquisition import AcquisitionRequest, AcquisitionRouter, AcquisitionStrategy, ProviderRegistration',
              1,
          )
          text = text.replace(
              'from .jobs import JobRunner\n',
              'from .jobs import JobRunner\nfrom .policy import PolicyError, ensure_within_root\n',
              1,
          )
          text = text.replace('from .storage import utc_now', 'from .storage import new_id, utc_now', 1)
          text = text.replace(
              'from .service import ResourceService as _LegacyResourceService',
              'from .service import (\n    ACQUISITION_ABORT_CODES,\n    PERSISTED_ASSET_ROLES,\n    PUBLIC_JOB_FAILURE_CODES,\n    ResourceService as _LegacyResourceService,\n)',
              1,
          )

          marker = '    @staticmethod\n    def _public_acquisition_outcome('
          if marker not in text:
              raise SystemExit('simple_service outcome marker missing')
          runner = r'''    def _run_download_job(self, job_id: str, cancel_event: threading.Event) -> None:
        """Execute the immutable JobItem route without legacy authority slots."""

        job = self.store.get_job(job_id)
        if job is None:
            return
        try:
            self._raise_for_cancel_event(job_id, cancel_event)
            self._start_download_job_execution(job_id)
            job_items = self.store.get_job_items(job_id)

            usable_primary_count = 0
            processed_count = 0
            saw_partial = False
            total_items = len(job_items)
            for item in job_items:
                resource_id = str(item["resource_id"])
                self._raise_for_cancel_event(job_id, cancel_event)
                self.store.start_acquisition_outcome(
                    job_id,
                    resource_id,
                    metadata={"attempt": 1},
                )
                representation = item.get("representation")
                if not isinstance(representation, dict):
                    raise DomainError(
                        "PLAN_BINDING_CONFLICT",
                        "任务执行项缺少已确认的资源表示",
                        details={"resource_id": resource_id},
                    )
                selected_container = representation.get("selected_container")
                if not isinstance(selected_container, str) or not selected_container:
                    raise DomainError(
                        "PLAN_BINDING_CONFLICT",
                        "任务执行项缺少已确认的容器",
                        details={"resource_id": resource_id},
                    )
                request = AcquisitionRequest(
                    job_id=job_id,
                    resource=item["resource"],
                    strategy=item["strategy"],
                    provider_id=item["provider_id"],
                    provider_version=item["provider_version"],
                    planned_scope=item["planned_scope"],
                    representation_id=item["representation_id"],
                    preferred_container=selected_container,
                    cancel_event=cancel_event,
                    jobs_root=self.settings.jobs_dir.resolve(),
                )
                acquisition = self.acquisition_router.acquire(request)

                expected_actual = (
                    str(item["planned_scope"]),
                    str(item["strategy"]),
                    str(item["provider_id"]),
                    str(item["provider_version"]),
                )
                observed_actual = (
                    acquisition.actual_scope,
                    acquisition.strategy.kind,
                    acquisition.provider_id,
                    acquisition.provider_version,
                )
                has_actual_provider_facts = any(
                    value is not None
                    for value in (
                        acquisition.actual_scope,
                        acquisition.provider_id,
                        acquisition.provider_version,
                    )
                )
                if has_actual_provider_facts and observed_actual != expected_actual:
                    raise DomainError(
                        "PLAN_BINDING_CONFLICT",
                        "获取结果与 JobItem 的 Provider 路线不一致",
                        details={"resource_id": resource_id},
                    )
                actual_for_outcome = (
                    expected_actual if observed_actual == expected_actual else (None, None, None, None)
                )

                if not acquisition.ok or acquisition.bundle is None:
                    failure = acquisition.failure
                    failure_code = failure.code if failure is not None else "DOWNLOAD_FAILED"
                    failure_message = failure.message if failure is not None else "获取任务没有产生可用结果"
                    failure_retryable = bool(failure.retryable) if failure is not None else False
                    failure_details = dict(failure.details) if failure is not None else {}
                    if failure_code == "JOB_CANCELLED":
                        if self._job_cancellation_is_persisted(job_id):
                            raise DomainError("JOB_CANCELLED", "任务已取消")
                        self.store.audit(
                            str(job["flow_id"]),
                            "download.provider_cancel_rejected",
                            job_id,
                            {"resource_id": resource_id, "provider_id": str(item["provider_id"])},
                        )
                        failure_code = "DOWNLOAD_FAILED"
                        failure_message = "资源获取提供方异常终止"
                        failure_retryable = False
                        failure_details = {}
                    self._raise_for_cancel_event(job_id, cancel_event)
                    self.store.persist_failed_asset_bundle(
                        job_id,
                        resource_id,
                        failure={
                            "code": failure_code,
                            "message": failure_message,
                            "retriable": failure_retryable,
                            "details": failure_details,
                            "item_position": 0,
                            "item_role": "primary",
                        },
                    )
                    self.store.complete_acquisition_outcome(
                        job_id,
                        resource_id,
                        status="failed",
                        actual_scope=actual_for_outcome[0],
                        actual_strategy=actual_for_outcome[1],
                        actual_provider_id=actual_for_outcome[2],
                        actual_provider_version=actual_for_outcome[3],
                        failure_code=failure_code,
                        failure_message=failure_message,
                        retriable=failure_retryable,
                        metadata={"item_failure_count": 1},
                    )
                    processed_count += 1
                    saw_partial = True
                    self._update_download_job_progress(
                        job_id, int((processed_count / total_items) * 100)
                    )
                    if failure_code in ACQUISITION_ABORT_CODES:
                        raise DomainError(
                            failure_code,
                            failure_message,
                            retryable=failure_retryable,
                            details=failure_details,
                        )
                    continue

                if observed_actual != expected_actual:
                    raise DomainError(
                        "PLAN_BINDING_CONFLICT",
                        "成功获取缺少与 JobItem 一致的实际 Provider 路线",
                        details={"resource_id": resource_id},
                    )

                artifacts = acquisition.bundle.artifacts
                if acquisition.strategy is AcquisitionStrategy.WEB_MATERIALIZE:
                    primary = acquisition.bundle.primary
                    artifacts = (primary,) if primary is not None else ()
                item_specs: list[dict[str, Any]] = []
                for position, artifact in enumerate(artifacts):
                    try:
                        ensure_within_root(artifact.path.resolve(), self.settings.jobs_dir)
                    except PolicyError as exc:
                        raise DomainError("POLICY_DENIED", str(exc)) from exc
                    if not artifact.path.is_file():
                        raise DomainError("CONTENT_VALIDATION_FAILED", "获取器没有产生受控文件")
                    artifact_data = artifact.to_dict(include_path=False)
                    role = "primary" if artifact.primary else str(artifact.role)
                    if role not in PERSISTED_ASSET_ROLES:
                        role = "attachment"
                    metadata = dict(artifact_data.get("metadata") or {})
                    if artifact.item_key:
                        metadata["item_key"] = artifact.item_key
                    item_specs.append(
                        {
                            "position": position,
                            "role": role,
                            "status": "ready",
                            "required": bool(artifact.required or artifact.primary),
                            "metadata": metadata,
                            "local_path": str(artifact.path.resolve()),
                            "byte_size": artifact.byte_size,
                            "media_type": artifact.media_type,
                            "sha256": artifact.sha256,
                            "filename": artifact.filename,
                        }
                    )

                failure_specs: list[dict[str, Any]] = []
                for failure in acquisition.item_failures:
                    position = len(item_specs)
                    role = failure.role or "attachment"
                    if role not in PERSISTED_ASSET_ROLES or role == "primary":
                        role = "attachment"
                    metadata = dict(failure.metadata)
                    metadata["item_key"] = failure.item_key
                    item_specs.append(
                        {
                            "position": position,
                            "role": role,
                            "status": "failed",
                            "required": bool(failure.required),
                            "metadata": metadata,
                        }
                    )
                    failure_specs.append(
                        {
                            "code": failure.code,
                            "message": failure.message,
                            "retriable": failure.retryable,
                            "details": dict(failure.details),
                            "item_position": position,
                            "item_role": role,
                        }
                    )
                if not any(
                    spec["role"] == "primary" and spec["status"] == "ready"
                    for spec in item_specs
                ):
                    raise DomainError(
                        "CONTENT_VALIDATION_FAILED",
                        "获取任务没有产生可持久化的 primary 资产",
                    )

                bundle_completion = (
                    "partial"
                    if failure_specs or acquisition.completion == "partial"
                    else "complete"
                )
                bundle = self.store.persist_asset_bundle(
                    job_id,
                    resource_id,
                    item_specs=item_specs,
                    failures=failure_specs,
                    completion=bundle_completion,
                )
                bundle_asset_ids = [
                    str(bundle_item["asset_id"])
                    for bundle_item in bundle.get("items", [])
                    if isinstance(bundle_item, dict)
                    and bundle_item.get("status") == "ready"
                    and isinstance(bundle_item.get("asset"), dict)
                    and bundle_item["asset"].get("asset_id")
                ]
                if not bundle_asset_ids:
                    raise DomainError("CONTENT_VALIDATION_FAILED", "资产包没有保存可用资产")
                self.store.complete_acquisition_outcome(
                    job_id,
                    resource_id,
                    status="partial" if bundle_completion == "partial" else "succeeded",
                    actual_scope=expected_actual[0],
                    actual_strategy=expected_actual[1],
                    actual_provider_id=expected_actual[2],
                    actual_provider_version=expected_actual[3],
                    bundle_id=str(bundle["bundle_id"]),
                    asset_ids=bundle_asset_ids,
                    metadata={
                        "completion": bundle_completion,
                        "item_failure_count": len(failure_specs),
                    },
                )
                usable_primary_count += 1
                processed_count += 1
                saw_partial = saw_partial or bundle_completion == "partial"
                self._update_download_job_progress(
                    job_id, int((processed_count / total_items) * 100)
                )

            self._raise_for_cancel_event(job_id, cancel_event)
            if usable_primary_count:
                try:
                    self.store.finalize_job_success(job_id)
                except ValueError as exc:
                    if str(exc) != "job_cancelling":
                        raise
                    self.store.finalize_job_cancellation(job_id)
            else:
                self._finalize_download_job_failure(
                    job_id,
                    failure_code="DOWNLOAD_FAILED",
                    failure_message="所选资源均未产生可用 primary 资产",
                    retriable=saw_partial,
                )
        except DomainError as exc:
            if self._job_cancellation_is_persisted(job_id):
                self.store.finalize_job_cancellation(
                    job_id,
                    failure_code="JOB_CANCELLED",
                    failure_message="任务已取消",
                )
            elif exc.code == "JOB_CANCELLED":
                self.store.audit(
                    str(job["flow_id"]),
                    "download.provider_cancel_rejected",
                    job_id,
                    {"source": "domain_error"},
                )
                self._finalize_download_job_failure(
                    job_id,
                    failure_code="DOWNLOAD_FAILED",
                    failure_message="资源获取提供方异常终止",
                    retriable=False,
                )
            else:
                self._finalize_download_job_failure(
                    job_id,
                    failure_code=exc.code if exc.code in PUBLIC_JOB_FAILURE_CODES else "INTERNAL_ERROR",
                    failure_message=exc.message,
                    retriable=exc.retryable,
                )
        except Exception as exc:
            if isinstance(exc, ValueError) and str(exc) == "job_cancelling":
                self.store.finalize_job_cancellation(
                    job_id,
                    failure_code="JOB_CANCELLED",
                    failure_message="任务已取消",
                )
                return
            incident_id = new_id("incident")
            self._finalize_download_job_failure(
                job_id,
                failure_code="INTERNAL_ERROR",
                failure_message=f"资源获取任务发生内部错误（事件 {incident_id}）",
                retriable=False,
            )
            self.store.audit(
                str(job["flow_id"]),
                "download.internal_error",
                job_id,
                {"incident_id": incident_id, "exception_type": type(exc).__name__},
            )

    def job_status(self, flow_id: str, job_id: str) -> dict[str, Any]:
        self._require_flow(flow_id)
        job = self.store.get_job(job_id)
        if job is None or job["flow_id"] != flow_id:
            raise DomainError("JOB_NOT_FOUND", "任务不存在")
        plan = self.store.get_plan(job["plan_id"])
        if plan is None:
            raise DomainError("PLAN_NOT_FOUND", "任务对应的下载计划不存在")
        bundles = self.store.get_asset_bundles_for_job(job_id)
        try:
            execution_by_resource = {
                str(item["resource_id"]): item for item in self.store.get_job_items(job_id)
            }
        except RuntimeError:
            execution_by_resource = {}
        outcomes = [
            self._public_acquisition_outcome(
                outcome, execution_by_resource.get(str(outcome["resource_id"]))
            )
            for outcome in self.store.get_acquisition_outcomes_for_job(job_id)
        ]
        assets = []
        for asset_id in job["asset_ids"]:
            asset = self.store.get_asset(asset_id)
            if asset is not None and asset["status"] == "ready":
                assets.append(self._public_asset(asset))
        failures = self._public_bundle_failures(bundles)[:50]
        if not failures and job["error"]:
            public_error = self._public_stored_job_error(job["error"])
            failures = [public_error] if public_error is not None else []
        result = {
            "job_id": job_id,
            "flow_id": job["flow_id"],
            "plan_id": plan["plan_id"],
            "presentation_id": plan["presentation_id"],
            "presented_version": int(plan["presented_version"]),
            "selection_version": int(plan["selection_version"]),
            "selection_digest": plan["selection_digest"],
            "plan_digest": plan.get("plan_digest") or "",
            "status": job["status"],
            "progress": {
                "completed_items": min(
                    len(
                        [
                            outcome
                            for outcome in outcomes
                            if outcome["status"] in {"succeeded", "partial", "failed", "cancelled"}
                        ]
                    ),
                    len(plan["resource_ids"]),
                ),
                "total_items": len(plan["resource_ids"]),
                "percent": job["progress"],
            },
            "assets": assets,
            "failures": failures,
            "outcomes": outcomes,
            "updated_at": job["updated_at"],
        }
        completion = self._job_completion(job, bundles)
        if completion is not None:
            result["completion"] = completion
        return result

'''
          text = text.replace(marker, runner + marker, 1)
          service_path.write_text(text, encoding='utf-8')

          # 4) Upgrade the focused tests to require physical cleanup, not empty compatibility fields.
          test_path = root / 'mcp/education-resources/tests/test_acquisition_simplification_0037.py'
          text = test_path.read_text(encoding='utf-8')
          request_pattern = re.compile(
              r'    def test_request_exposes_only_execution_facts\(self\) -> None:\n.*?\n    def test_primary_article_webpage_routes_to_materializer',
              re.S,
          )
          request_replacement = r'''    def test_request_has_no_legacy_authority_slots(self) -> None:
        with tempfile.TemporaryDirectory() as directory:
            request = AcquisitionRequest(
                job_id="job_test_0037",
                resource={"resource_id": "res_test_0037", "platform": "generic"},
                strategy="web_materialize",
                provider_id="generic-web-materializer",
                provider_version="1.0.0",
                planned_scope="primary_resource",
                representation_id="repr_test_0037",
                preferred_container="html",
                cancel_event=threading.Event(),
                jobs_root=Path(directory),
            )
            for deleted in (
                "binding_digest",
                "source_fingerprint",
                "capability_id",
                "descriptor_digest",
                "readiness_snapshot_id",
                "readiness_digest",
                "eligibility_id",
                "eligibility_digest",
            ):
                self.assertFalse(hasattr(request, deleted), deleted)
            with self.assertRaises(TypeError):
                AcquisitionRequest(
                    job_id="job_test_legacy_kwarg",
                    resource={"resource_id": "res_test_legacy_kwarg", "platform": "generic"},
                    strategy="web_materialize",
                    provider_id="generic-web-materializer",
                    provider_version="1.0.0",
                    planned_scope="primary_resource",
                    representation_id="repr_test_legacy_kwarg",
                    preferred_container="html",
                    cancel_event=threading.Event(),
                    jobs_root=Path(directory),
                    binding_digest="legacy",  # type: ignore[call-arg]
                )
        public = request.to_dict()
        self.assertEqual(public["planned_scope"], "primary_resource")
        self.assertEqual(public["strategy"], "web_materialize")

    def test_primary_article_webpage_routes_to_materializer'''
          text, count = request_pattern.subn(request_replacement, text, count=1)
          if count != 1:
              raise SystemExit('request test replacement failed')
          text = text.replace(
              '    def test_migration_8_has_business_tables_without_authority_digests(self) -> None:',
              '    def test_migration_9_drops_legacy_authority_tables(self) -> None:',
              1,
          )
          text = text.replace('        self.assertEqual(version, 8)', '        self.assertEqual(version, 9)', 1)
          needle = '        self.assertTrue({"acquisition_plan_items", "job_items", "execution_outcomes"} <= tables)\n'
          addition = '''        self.assertTrue({"acquisition_plan_items", "job_items", "execution_outcomes"} <= tables)\n        self.assertTrue(\n            {\n                "capability_readiness_snapshots",\n                "eligibility_decisions",\n                "download_plan_items",\n                "job_execution_items",\n                "acquisition_outcomes",\n            }.isdisjoint(tables)\n        )\n'''
          if needle not in text:
              raise SystemExit('migration table assertion marker missing')
          text = text.replace(needle, addition, 1)
          test_path.write_text(text, encoding='utf-8')

          # 5) Remove the standalone acquisition-authority contract artifacts.
          obsolete = [
              'mcp/education-resources/contracts/capabilities/capability-descriptors.json',
              'mcp/education-resources/contracts/schemas/capability-descriptor.schema.json',
              'mcp/education-resources/contracts/schemas/capability-descriptors.schema.json',
              'mcp/education-resources/contracts/schemas/deployment-readiness.schema.json',
              'mcp/education-resources/contracts/schemas/eligibility-decision.schema.json',
          ]
          for relative in obsolete:
              path = root / relative
              if path.exists():
                  path.unlink()

          contracts_readme = root / 'mcp/education-resources/contracts/README.md'
          text = contracts_readme.read_text(encoding='utf-8')
          compat_pattern = re.compile(r'## 兼容目录\n.*?\n## 目录导航', re.S)
          compat_replacement = '''## 已清理的旧获取契约\n\n0037 第二阶段已经物理删除独立 Capability Descriptor、Deployment Readiness 与 Eligibility Decision 的 catalog/schema。\n这些对象只存在于 Git 历史和归档计划中，不再属于 current contract 目录，也不能被新代码重新解释为 acquisition source of truth。\n\n## 目录导航'''
          text, count = compat_pattern.subn(compat_replacement, text, count=1)
          if count != 1:
              raise SystemExit('contracts README compatibility section replacement failed')
          text = text.replace('├── capabilities/              # 0037 兼容期历史输入；非 Active 获取真值\n', '')
          contracts_readme.write_text(text, encoding='utf-8')

          platform_readme = root / 'mcp/education-resources/contracts/platforms/README.md'
          text = platform_readme.read_text(encoding='utf-8')
          acquisition_pattern = re.compile(
              r'## Acquisition 执行权威（Tool catalog 1\.5）\n.*?\n## Identity profile',
              re.S,
          )
          acquisition_replacement = '''## Acquisition 运行时路由（Tool catalog 1.6）\n\nPlatform Registry 只描述平台身份、Search/Inspect 与历史能力声明。当前 acquisition 不再维护独立 Capability Descriptor catalog。\n执行路线由 `acquisition/planner.py` 中的小型 `ProviderSpec` 与当前部署的 exact Provider registration 共同决定，并且必须以当前 Resolution/Representation 为输入。\n\n当前简化路线至少包括：\n\n| representation | strategy / scope | exact Provider |\n|---|---|---|\n| generic document primary | `direct_file` / `primary_resource` | `generic-direct@1.0.0` |\n| generic webpage primary | `web_materialize` / `primary_resource` | `generic-web-materializer@1.0.0` |\n| generic webpage landing | `web_materialize` / `landing_page` | `generic-web-materializer@1.0.0` |\n| SmartEdu document primary | `direct_file` / `primary_resource` | `smartedu-resource@1.0.0`（仅实际注册时） |\n\nPrepare 根据 fresh Representation 生成 PlanItem；Start 重新读取当前 Resolution，核对 representation 未漂移，并确认 Plan 中的 exact Provider 仍注册且支持相同 scope/strategy。该检查不生成 Readiness Snapshot、Eligibility ID 或 binding digest。\n\nProvider 失败后不切换 Generic/其他 Provider、scope 或 strategy。Bilibili、Douyin、Ximalaya、Anna/Libgen 等路线如果没有当前 `ProviderSpec + Representation + exact Provider`，就保持结构化 blocked/unsupported，不能从 Registry 的历史 `acquire=true` 推导执行能力。\n\n未来新增 acquisition route 时，应最小化地新增/调整 ProviderSpec、Inspector 产生的 Representation、Provider registration 和业务回归测试；不得重新建立 Descriptor → Readiness → Eligibility → digest 状态链。\n\n## Identity profile'''
          text, count = acquisition_pattern.subn(acquisition_replacement, text, count=1)
          if count != 1:
              raise SystemExit('platform README acquisition section replacement failed')
          text = text.replace(
              '3. 更新 Skill 的 `references/platform-capabilities.md`；平台身份、检索与历史声明仍以本 JSON 为机器权威，acquisition 执行能力则以独立 Capability Descriptor catalog 为权威。',
              '3. 平台身份、检索与历史声明仍以本 JSON 为机器权威；acquisition 执行路线以当前 ProviderSpec、Representation 和 exact Provider registration 为准。',
          )
          text = text.replace(
              '6. 新增 acquisition route 必须修改独立的 Capability Descriptor catalog、readiness/eligibility\n   与绑定测试；不得通过改 `acquire=true` 或 `acquisition.strategies` 扩大当前执行能力。',
              '6. 新增 acquisition route 必须同步 ProviderSpec、Inspector Representation、exact Provider registration 与业务测试；不得通过改 `acquire=true` 或 `acquisition.strategies` 扩大当前执行能力。',
          )
          platform_readme.write_text(text, encoding='utf-8')

          # Hard assertions: active runner/request no longer depend on compatibility slots.
          service_text = service_path.read_text(encoding='utf-8')
          for token in (
              'get_job_execution_items',
              'execution_binding_digest',
              'readiness_snapshot_id',
              'eligibility_id',
              'capability_scope',
          ):
              if token in service_text:
                  raise SystemExit(f'legacy token remains in simple_service: {token}')
          simple_text = simple_path.read_text(encoding='utf-8')
          for token in (
              'binding_digest',
              'source_fingerprint',
              'capability_id',
              'readiness_snapshot_id',
              'eligibility_id',
          ):
              if token in simple_text:
                  raise SystemExit(f'legacy request slot remains: {token}')
          storage_text = storage_path.read_text(encoding='utf-8')
          for token in ('get_job_execution_items =', 'item["capability_scope"]', 'item[key] = None'):
              if token in storage_text:
                  raise SystemExit(f'legacy JobItem adapter remains: {token}')
          for relative in obsolete:
              if (root / relative).exists():
                  raise SystemExit(f'obsolete contract still exists: {relative}')
          PY
      - name: Install service
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ./mcp/education-resources pytest
      - name: Compile current package
        run: python -m compileall -q mcp/education-resources/src
      - name: Parse current JSON contracts
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          for path in Path('mcp/education-resources/contracts').rglob('*.json'):
              json.loads(path.read_text(encoding='utf-8'))
          PY
      - name: Validate simplified state and runtime
        working-directory: mcp/education-resources
        run: |
          python -m pytest -q tests/test_acquisition_simplification_0037.py
          python -m pytest -q tests/test_mcp_stdio.py
      - name: Verify current contract has no deleted authority artifacts
        run: |
          test ! -e mcp/education-resources/contracts/capabilities/capability-descriptors.json
          test ! -e mcp/education-resources/contracts/schemas/capability-descriptor.schema.json
          test ! -e mcp/education-resources/contracts/schemas/capability-descriptors.schema.json
          test ! -e mcp/education-resources/contracts/schemas/deployment-readiness.schema.json
          test ! -e mcp/education-resources/contracts/schemas/eligibility-decision.schema.json
          ! git grep -nE 'Capability Descriptor -> Deployment Readiness|authority_digest|execution_binding_digest|readiness_snapshot_id|eligibility_id' -- mcp/education-resources/contracts/README.md mcp/education-resources/contracts/platforms/README.md
      - name: Commit successful cutover
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f .github/workflows/0037-part2-cutover.yml .github/workflows/0037-part2-audit.yml
          git add mcp/education-resources/src mcp/education-resources/tests/test_acquisition_simplification_0037.py mcp/education-resources/contracts
          git commit -m 'refactor: remove acquisition authority compatibility layer'
          git push origin HEAD:codex/growth-resource-taxonomy-rework
