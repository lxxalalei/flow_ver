# Legacy

本目录保存迁移前实现，仅用于审计、对等测试和显式回滚，不属于当前 OpenClaw
active 工作区能力。

- 不从本目录发现或安装 Skill。
- 不让模型在 MCP 正常时调用旧脚本。
- 不在此处新增平台能力；新能力进入 `mcp/education-resources/`。
- 旧代码仍可能包含未完成或高风险平台实现，不能视为生产可用。

当前快照位于 `skill-pipeline-v1/`。
