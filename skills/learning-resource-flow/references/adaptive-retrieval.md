# Adaptive Retrieval（兼容入口）

该主题已合并到 [`retrieval.md`](retrieval.md)。

新的生产规则、SemanticReview、Gap、StopDecision、SearchDirection、轮次预算和 ResultSet extend 说明只在 `retrieval.md` 维护；权威边界见 [`docs/RETRIEVAL_AUTHORITY.md`](../../../docs/RETRIEVAL_AUTHORITY.md)。

保留本文件仅用于兼容旧链接，不再在这里新增规则。
