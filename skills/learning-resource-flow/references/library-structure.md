# Library Structure（兼容入口）

Asset、AssetBundle、归档、分类、资料库搜索和去重规则已合并到 [`library.md`](library.md)。

分类机器事实仍以 [`mcp/education-resources/contracts/taxonomy/`](../../../mcp/education-resources/contracts/taxonomy/) 为准。

保留本文件仅用于兼容旧链接，不再在这里新增资料库规则。
