# Candidate Judgment（兼容入口）

候选语义审查已合并到 [`retrieval.md`](retrieval.md)。

新的 relevance、usefulness、target_fit、constraint_fit、substantive、evidence_level 规则和展示边界只在 `retrieval.md` 维护。

保留本文件仅用于兼容旧链接，不再在这里新增规则。
